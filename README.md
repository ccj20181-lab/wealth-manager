# 个人财富管理网站

全面的个人财富管理平台，用于管理基金投资、现金流、理财目标等。

## 技术栈

- **前端**: React + Vite + TypeScript
- **样式**: Tailwind CSS v4 + shadcn/ui
- **状态管理**: Zustand + React Query
- **图表**: Recharts
- **后端**: Supabase (PostgreSQL + Auth)
- **部署**: Cloudflare Pages

## 功能模块

- 📊 **资产仪表盘** - 总资产概览、净值走势、资产配置饼图
- 📈 **基金投资** - 持仓管理、交易记录、定投计划
- 💰 **现金流管理** - 收支记录、分类统计、预算管理
- 🎯 **投资目标** - 目标设定、进度追踪、提醒功能
- 🔐 **用户认证** - 登录、注册、密码重置

## 本地开发

```bash
# 1. 克隆项目
git clone <repository-url>
cd wealth-manager

# 2. 安装依赖
npm install

# 3. 配置环境变量
cp .env.example .env
# 编辑 .env 文件，填入 Supabase 凭据

# 4. 启动开发服务器
npm run dev
```

## 构建部署

```bash
# 构建生产版本
npm run build

# 预览构建结果
npm run preview
```

## 项目结构

```
src/
├── components/
│   ├── ui/           # 基础 UI 组件
│   ├── auth/         # 认证组件
│   ├── charts/       # 图表组件
│   ├── dashboard/    # 仪表盘组件
│   ├── funds/        # 基金模块组件
│   ├── cashflow/     # 现金流组件
│   └── goals/        # 目标管理组件
├── hooks/            # React Query hooks
├── layouts/          # 布局组件
├── lib/              # 工具库
├── pages/            # 页面组件
├── services/api/     # API 服务层
├── stores/           # Zustand 状态管理
└── types/            # TypeScript 类型
```

## 数据库

使用 Supabase 作为后端，数据库迁移文件位于 `supabase/migrations/` 目录。

## 许可证

MIT
